{
  "dependencies": {
    "express": "^4.18.2",
    "pkg": "^5.8.1"
  },
  "scripts": {
    "build": "pkg squadMortarServer.js -t=win && create-nodew-exe squadMortarServer.exe squadMortarServerSilent.exe"
  }
}
